# Core (reproducible pipeline: EDA + Model 2 + report, local CSV only)
pandas
numpy
scipy
statsmodels
matplotlib

# Optional — Model 1 (streaming the remote h5ad without downloading it)
# install only if you will run `make spike` / `make edges` / `make repro-meta`:
#   pip install h5py s3fs fsspec

# Optional — side analysis (transcriptional fingerprints, `make fingerprints`)
# install only if you will run the fingerprint analysis:
#   pip install scikit-learn

# Optional — read-only API (Regulator Atlas)
# install only if you will run the API:  uvicorn api.main:app --port 8000
fastapi
uvicorn[standard]

# Optional — empirical regulatory operator analysis (`make operator`)
#   pip install tensorly pytest h5py s3fs fsspec
